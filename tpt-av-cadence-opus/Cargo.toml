[package]
name = "tpt-av-cadence-opus"
description = "Opus (RFC 6716) decoder for the tpt-cadence audio codec suite."
readme = "README.md"
keywords = ["audio", "opus", "codec", "decoder"]
categories = ["multimedia::audio", "multimedia::encoding"]
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
tpt-av-cadence-core = { workspace = true }
tpt-av-cadence-ogg = { workspace = true }
log = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "celt_round_trip"
harness = false
